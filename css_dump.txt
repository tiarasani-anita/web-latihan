
======================================================================
RULES DENGAN background-clip: text
======================================================================
display: inline-block;
    font-family: 'Sora', sans-serif;
    font-size: clamp(2rem, 5.2vw, 3.5rem);
    font-weight: 800;
    letter-spacing: 1.5px;
    line-height: 1.1;
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    /* Gabungan: gradasi bergerak + melayang/berputar 3D tak terbatas */
    animation: floatName 7s ease-in-out infinite, gradientMove 8s linear infinite;
    filter: drop-shadow(0 0 12px rgba(0, 240, 255, 0.28))
            drop-shadow(0 0 30px rgba(255, 46, 196, 0.16));
    will-change: transform;
}

display: inline-block;
    font-family: 'Sora', sans-serif;
    font-size: clamp(2rem, 5.2vw, 3.5rem);
    font-weight: 800;
    letter-spacing: 1.5px;
    line-height: 1.1;
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    /* Gabungan: gradasi bergerak + melayang/berputar 3D tak terbatas */
    animation: floatName 7s ease-in-out infinite, gradientMove 8s linear infinite;
    filter: drop-shadow(0 0 12px rgba(0, 240, 255, 0.28))
            drop-shadow(0 0 30px rgba(255, 46, 196, 0.16));
    will-change: transform;
}

/* Lampu modern di tepi huruf — gradasi tetap hidup, glow lebih premium. */
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: gradientMove 8s linear infinite, floatName 7s ease-in-out infinite;
    filter: drop-shadow(0 0 3px rgba(255, 255, 255, 0.45))
            drop-shadow(0 0 8px rgba(0, 240, 255, 0.5))
            drop-shadow(0 0 18px rgba(255, 46, 196, 0.35))
            drop-shadow(0 0 36px rgba(139, 92, 246, 0.25));
}

/* Lampu modern di tepi huruf — gradasi tetap hidup, glow lebih premium. */
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: gradientMove 8s linear infinite, floatName 7s ease-in-out infinite;
    filter: drop-shadow(0 0 3px rgba(255, 255, 255, 0.45))
            drop-shadow(0 0 8px rgba(0, 240, 255, 0.5))
            drop-shadow(0 0 18px rgba(255, 46, 196, 0.35))
            drop-shadow(0 0 36px rgba(139, 92, 246, 0.25));
}

background: linear-gradient(90deg, var(--neon-cyan), var(--neon-magenta));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.25));
}

background: linear-gradient(90deg, var(--neon-cyan), var(--neon-magenta));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.25));
}


======================================================================
RULES DENGAN filter: drop-shadow
======================================================================
/* Gradasi tetap hidup, tapi glow difilter jauh lebih lembut */
    filter: drop-shadow(0 0 4px rgba(0, 240, 255, 0.4))
            drop-shadow(0 0 10px rgba(255, 46, 196, 0.3));
}

filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.25));
}

display: inline-block;
    font-family: 'Sora', sans-serif;
    font-size: clamp(2rem, 5.2vw, 3.5rem);
    font-weight: 800;
    letter-spacing: 1.5px;
    line-height: 1.1;
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    /* Gabungan: gradasi bergerak + melayang/berputar 3D tak terbatas */
    animation: floatName 7s ease-in-out infinite, gradientMove 8s linear infinite;
    filter: drop-shadow(0 0 12px rgba(0, 240, 255, 0.28))
            drop-shadow(0 0 30px rgba(255, 46, 196, 0.16));
    will-change: transform;
}

/* Lampu modern di tepi huruf — gradasi tetap hidup, glow lebih premium. */
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: gradientMove 8s linear infinite, floatName 7s ease-in-out infinite;
    filter: drop-shadow(0 0 3px rgba(255, 255, 255, 0.45))
            drop-shadow(0 0 8px rgba(0, 240, 255, 0.5))
            drop-shadow(0 0 18px rgba(255, 46, 196, 0.35))
            drop-shadow(0 0 36px rgba(139, 92, 246, 0.25));
}

background: linear-gradient(90deg, var(--neon-cyan), var(--neon-magenta));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.25));
}

filter: drop-shadow(0 0 8px var(--neon-cyan)); }

filter: drop-shadow(0 0 22px var(--neon-magenta)); }

filter: drop-shadow(0 0 8px var(--neon-cyan)); }

position: relative;
    z-index: 2;
    width: 90px;
    height: 90px;
    color: var(--neon-cyan);
    filter: drop-shadow(0 0 18px rgba(0, 240, 255, 0.7));
    transition: transform 0.5s ease, filter 0.5s ease;
}

transform: scale(1.18) rotate(-6deg);
    filter: drop-shadow(0 0 32px rgba(255, 46, 196, 0.8));
}

font-size: 3rem;
    margin-bottom: 14px;
    filter: drop-shadow(0 0 8px rgba(0, 240, 255, 0.4));
    animation: rotateGlow 5s linear infinite;
}

font-size: 2.4rem;
    margin-bottom: 14px;
    filter: drop-shadow(0 0 12px rgba(0, 240, 255, 0.4));
    transition: transform 0.4s ease;
}

position: relative;
    z-index: 2;
    font-size: 3.2rem;
    filter: drop-shadow(0 0 16px rgba(0, 240, 255, 0.5));
    transition: transform 0.5s ease;
}


======================================================================
RULES DENGAN -webkit-text-fill-color
======================================================================

======================================================================
RULES DENGAN color: transparent
======================================================================
display: inline-block;
    font-family: 'Sora', sans-serif;
    font-size: clamp(2rem, 5.2vw, 3.5rem);
    font-weight: 800;
    letter-spacing: 1.5px;
    line-height: 1.1;
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    /* Gabungan: gradasi bergerak + melayang/berputar 3D tak terbatas */
    animation: floatName 7s ease-in-out infinite, gradientMove 8s linear infinite;
    filter: drop-shadow(0 0 12px rgba(0, 240, 255, 0.28))
            drop-shadow(0 0 30px rgba(255, 46, 196, 0.16));
    will-change: transform;
}

/* Lampu modern di tepi huruf — gradasi tetap hidup, glow lebih premium. */
    background: linear-gradient(100deg, var(--neon-cyan) 0%, #7df9ff 30%, var(--neon-magenta) 65%, var(--neon-purple) 100%);
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: gradientMove 8s linear infinite, floatName 7s ease-in-out infinite;
    filter: drop-shadow(0 0 3px rgba(255, 255, 255, 0.45))
            drop-shadow(0 0 8px rgba(0, 240, 255, 0.5))
            drop-shadow(0 0 18px rgba(255, 46, 196, 0.35))
            drop-shadow(0 0 36px rgba(139, 92, 246, 0.25));
}

background: linear-gradient(90deg, var(--neon-cyan), var(--neon-magenta));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.25));
}

position: relative;
    border-color: transparent;
}

